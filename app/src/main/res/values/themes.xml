<resources>
    <style name="Theme.LudoGame" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Fallback just to avoid build error, Compose handles theming -->
        <item name="android:colorPrimary">@android:color/holo_blue_light</item>
        <item name="android:colorSecondary">@android:color/holo_green_light</item>
    </style>
</resources>
